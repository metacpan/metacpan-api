cpan t/var/tmp/fakecpan
source_base t/var/tmp/source

<model CPAN>
    servers __ENV(ES)__
</model CPAN>

<model User>
    servers __ENV(ES)__
</model User>

<plugin Session>
    servers __ENV(ES)__
</plugin>

<controller User::Turing>
    captcha_class Captcha::Mock
    private_key testing
</controller>
